export default function TagItem({ choise }) {
  return (
    <div className='m-1 py-1 px-2 rounded-full bg-gradient-to-r from-primary to-secondary text-xs text-white font-semibold'>
      {choise}
    </div>
  );
}
